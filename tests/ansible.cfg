[defaults]
# Resolve `ansible-role-pssid-data-pipeline` (this repo) as a role by searching
# its parent directory. Run ansible-playbook from within this tests/ directory.
roles_path = ../..
inventory = inventory
