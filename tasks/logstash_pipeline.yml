---
- name: Stage the Logstash pipeline (logstash.conf + ruby filters)
  ansible.builtin.copy:
    src: logstash-pipeline/
    dest: "{{ pssid_pipeline_dir }}/logstash-pipeline/"
    owner: "{{ pssid_pipeline_user }}"
    group: "{{ pssid_pipeline_user }}"
    mode: "0644"
  become: true
